#ifndef TESTER_BUFFER_RING_BUFFER_INDEX_H
#define TESTER_BUFFER_RING_BUFFER_INDEX_H

namespace Test{
	// ------------------------------------------------------------
	/** function to initialize and release a ring buffer */
	int tester_buffer_initialize_and_release();
	// ------------------------------------------------------------
	int tester_buffer_ring_buffer_index();
	// ------------------------------------------------------------
}
#endif TESTER_BUFFER_RING_BUFFER_INDEX_H